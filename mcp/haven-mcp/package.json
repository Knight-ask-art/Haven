{
  "name": "haven-mcp-server",
  "version": "0.1.0-beta.1",
  "private": true,
  "description": "栖阅 Haven 的 MCP server：只读上下文 + 受控提案创建（无 Apply/Approve/Write）",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "haven-mcp-server": "dist/index.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "node dist/index.js",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "1.30.0",
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^26.5.1",
    "typescript": "~7.0.2",
    "vitest": "^5.0.0"
  }
}
