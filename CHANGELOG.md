# 栖阅 Haven 变更记录

这里记录公开版本面向使用者的支持范围、限制和兼容说明。工程内部计划、验收证据和本地诊断输出不放在公开仓库中。

## v0.1.0-beta.1 · 首个公开内测预览

### 支持内容

- Windows 本地优先媒体库：登记本地目录、扫描内容，并以 Work → Edition → MediaItem 组织媒体。
- 视频播放、阅读器、漫画阅读器、收藏、进度、历史、标记和资源级偏好。
- EPUB 目录与搜索定位；TXT、Markdown、PDF 和 HTML/Article 的受控阅读路径。
- CBZ 归档和直接图片目录；漫画支持单页、双页、条漫、RTL/LTR、预加载和坏页隔离。
- 本地全文搜索、来源注册、真实内置来源搜索，以及局部来源失败时的 Partial/重试状态。
- Offline Download 队列、重启恢复、空间准入和统一通知中心。
- 播放器 `Ctrl+Shift+S` 截图：后端受限 JPEG 分块上传、原生保存对话框和默认截图目录。
- 严格 CSP、最小 Tauri Capability、生成绑定一致性检查和脱敏日志。

### Windows 系统要求

- v0.1.0-beta.1 仅提供 Windows 桌面版本；Android/iOS 不在本版本发布范围内。
- Windows 10 1809 或更新版本（x64）或 Windows 11。
- 已安装并保持更新的 Microsoft Edge WebView2 Evergreen Runtime。
- 运行已发布安装包不需要 Node.js 或 Rust；从源码构建需要 Node.js 22 和仓库锁定的 Rust 1.94.1。各 crate 的 `rust-version = 1.85` 仍表示代码最低版本，但当前锁定依赖要求更高的构建工具链。
- 建议至少 4 GB 可用内存，并为媒体库、缓存和离线资源预留足够磁盘空间。

### 支持的格式

| 内容类型 | v0.1.0 支持范围 |
| --- | --- |
| 视频 | `mp4`、`mkv`、`avi`、`mov`、`wmv`、`webm`、`flv`、`ts`、`m2ts`；实际解码能力取决于 WebView2/系统媒体栈 |
| 图书/文档 | `txt`、`md`、`markdown`、`epub`、`pdf` |
| Article | `html`、`htm` |
| 漫画 | `cbz` 和受控图片目录；目录中的图片仅接受 JPEG、PNG、WebP |

### 内置来源

所有内置来源都已经接入真实搜索参与者；来源的搜索、元数据、播放/下载和配置边界见 [SOURCES.md](SOURCES.md)。来源可能受上游限流、改版或临时故障影响，Haven 不承诺第三方 SLA。

### Artwork Cache 与默认封面

- 远程海报只由后端 Artwork Cache 受控抓取，前端不会直接请求第三方图片。
- 原图和 `w200`/`w400` 变体使用受限的 JPEG、PNG、WebP 校验、尺寸和大小上限；缓存文件以相对索引保存，可在文件缺失时自愈重建。
- 当来源没有海报、抓取失败或图片不符合安全策略时，界面从随应用发布的四类默认封面中选择一个：影视、图书、漫画、报刊文章。默认封面只作为显示回退，不会写入来源元数据，也不会覆盖用户媒体。
- 清理 Artwork Cache 只清理可再生图片缓存，不会删除原始媒体、离线资源、进度、收藏或标记。

### 已知限制

- 影视优化分支已接入受控外挂字幕的 SRT/VTT/SBV 编码归一化、轨道切换和资源生命周期，但尚未完成 runtime/release 四层验收，不能作为 `v0.1.0-beta.1` 已发布能力；音轨选择和 HEVC 解码控制仍未开放，硬件解码由 WebView2/系统管理，设置页不提供伪开关。
- 长时间播放、1000 页级 EPUB/漫画的长期内存和稳定性，以及 Downloads Worker 的极限并发、限速、断点恢复和磁盘异常，仍处于用户反馈阶段。
- TXT、Markdown、PDF、Article 的复杂真实样本兼容性会继续根据用户反馈改进。
- 某些 Provider 只提供元数据或候选资源，不保证每个条目都有海报、播放地址或下载权限。
- 影视远程 metadata/source/stream 请求为保证 DNS pinning 的连接目标不可被系统代理改写，使用直连策略（`no_proxy`）；企业代理或 TUN 环境可能因此无法拉取部分来源，属于已知限制。
- 运行独立 Tauri 程序时不依赖 `localhost:1420`；浏览器 Preview 仅用于 Mock/不可用分支演示。

### 尚未开放

OCR、翻译、AI、Sync、自动更新安装能力以及“清除全部本地数据”都需要独立的 Foundation、风险评审和桌面验收。本版本会安全显示不可用状态，不会通过只修改界面的 Demo 开关伪装成已完成。

### 升级与数据兼容

- v0.1.0-beta.1 是首个公开内测快照。应用首次启动会创建或升级本地 SQLite 数据库，并按顺序执行随版本提供的迁移；升级时不要删除应用数据目录。后续正式版固定使用 `v0.1.0`，不会与 beta 共用同一个版本号。
- 设置、媒体库、进度、收藏、历史、标记和离线资源由本机数据目录保存，后续版本会尽量保持向前兼容；不承诺跨版本降级后数据可写入。
- Artwork Cache、Provider 响应和缩略图是可再生数据，升级或清理后会在需要时重新构建。
- 默认封面和其他随应用发布的资源随安装包更新；它们不会改变用户原始媒体或业务记录。
- 从源码构建时请使用仓库锁定的 Node/Rust 版本，并先完成前端构建，再构建 Tauri custom-protocol 程序。

## 反馈阶段项目

以下项目不会在没有真实证据时写成 v0.1.0-beta.1 保证：长篇内容长期稳定性、其他格式的复杂样本、播放器长期自动行为和 Downloads Worker 的极限场景。欢迎通过 Issue 提供最小复现和脱敏诊断报告。

### 发布路线

- 内测：`v0.1.0-beta.1`，GitHub Release 标记为 prerelease。
- 正式版：`v0.1.0`，仅在内测反馈、签名制品和桌面验收完成后发布。
- Tauri Updater 按语义化版本比较 beta 与 stable；两者不使用同一个版本号。
